apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: deployment-application
  name: ollama
  namespace: app
spec:
  type: LoadBalancer
  selector:
    name: ollama
  ports:
  - port: 80
    targetPort: 11434  # Should match the containerPort in the Deployment
    protocol: TCP
